[project]
name = "character-asset-generator"
version = "0.1.0"
description = "LangChain pipeline turning a character brief into masked sprite assets"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.0",
    "langgraph>=1.0",
    "pillow>=10",
    "numpy>=1.26",
    "httpx>=0.28",
    # Vision is the mask's fallback and is imported at call time, so the rest of
    # the pipeline, and the tests, install anywhere.
    "pyobjc-framework-Vision>=10; sys_platform == 'darwin'",
    "pyobjc-framework-Quartz>=10; sys_platform == 'darwin'",
]

[project.scripts]
cag = "cag.cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cag"]

[tool.pytest.ini_options]
testpaths = ["tests"]
